{
  "name": "<%= name %>",
  "version": "1.0.0",
  "description": "<%= description %>",
  "author": "<%= author %>",
  "main": "index.js",
  "scripts": {<% if(lint==='yes'){ %>
    "lint": "tslint --fix src/**/*.ts",<% } %><% if(jest==='yes'){ %>
    "test": "jest",<% } %>
    "preprocess": "<% if(lint==='yes'){ %>npm run lint<% } %><% if(lint==='yes' && jest==='yes'){ %> && <% } %><% if(jest==='yes'){ %>npm run test<% } %><% if(lint==='no' && jest==='no'){ %>echo \"lint and jest are not available\"<% } %>",
    "dev": "watch \"npm run preprocess && clasp push\" ./src",
    "dev:local": "watch \"npm run preprocess\" ./src"
  },
  "license": "MIT",
  "devDependencies": {
    "watch": "latest"<% if(language==='ts'){ %>,
    "typescript": "latest",
    "@types/google-apps-script": "latest"<% } %><% if(lint==='yes'){ %>,
    "tslint": "latest",
    "prettier": "latest",
    "tslint-config-prettier": "latest",
    "tslint-plugin-prettier":  "latest"<% } %><% if(jest==='yes'){ %>,
    "jest": "latest",
    "@types/jest": "latest",
    "ts-jest": "latest"<% } %>
  }
}
